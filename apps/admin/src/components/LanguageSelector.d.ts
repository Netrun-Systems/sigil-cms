/**
 * LanguageSelector Component
 *
 * Displays available translations for a page and allows creating new ones.
 * Shows which languages exist (with links) and which are missing.
 */
interface LanguageSelectorProps {
    siteId: string;
    pageId: string;
    currentLanguage: string;
}
export declare function LanguageSelector({ siteId, pageId, currentLanguage }: LanguageSelectorProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=LanguageSelector.d.ts.map